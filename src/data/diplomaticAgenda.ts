import { DiplomaticEvent } from '../types';

/**
 * Conteúdos geridos e alimentados via Painel de Gestão (Admin).
 * O array inicial de código-fonte foi esvaziado para gestão 100% dinâmica.
 */
export const diplomaticEvents: DiplomaticEvent[] = [];
